/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'healing-hope-db164f5808.media.strapiapp.com', 'healing-hope-db164f5808.strapiapp.comhttps'], // Correct cloud media domain
  },
};

export default nextConfig;
